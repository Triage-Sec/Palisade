name: Tool Guard CI

on:
  push:
    branches: [main]
    paths:
      - "services/tool_guard/**"
      - "proto/tool_guard/**"
      - ".github/workflows/tool-guard-ci.yml"
  pull_request:
    paths:
      - "services/tool_guard/**"
      - "proto/tool_guard/**"
      - ".github/workflows/tool-guard-ci.yml"

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          cache-dependency-path: services/tool_guard/go.sum

      - uses: golangci/golangci-lint-action@v7
        with:
          version: v2.10
          working-directory: services/tool_guard

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          cache-dependency-path: services/tool_guard/go.sum

      - name: Run tests
        working-directory: services/tool_guard
        run: go test ./... -v -race -count=1

  build:
    needs: [lint, test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image (smoke test)
        run: docker build -f services/tool_guard/deploy/Dockerfile -t palisade-tool-guard:ci .
